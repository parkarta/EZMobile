//#include "stdafx.h"

#include "HexEdit.h"

//BEGIN_MESSAGE_MAP(CHexEdit, CEdit)
//	ON_WM_CHAR()
//	ON_WM_CREATE()
//END_MESSAGE_MAP()
//
//void CHexEdit::OnChar(UINT nChar, UINT nRepCnt, UINT nFlags) {
//	/* allow only hex chars */
//    if (nChar >= '0' && nChar <= '9' ||
//		nChar >= 'a' && nChar <= 'f' ||
//		nChar >= 'A' && nChar <= 'F' ||
//        nChar == VK_BACK
//		)
//		CEdit::OnChar(nChar, nRepCnt, nFlags);
//
//	/* shortcuts having CTRL can pass (copy, paste, undo, etc)*/
//	if (GetKeyState(VK_CONTROL) & 0x8000)
//		CEdit::OnChar(nChar, nRepCnt, nFlags);
//}	// void CHexEdit::OnChar(UINT nChar, UINT nRepCnt, UINT nFlags)

